[package]
name = "mercyos"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Forgiveness Eternal — no_std Post-Quantum + Hybrid Migration Cryptography for Android & Embedded"
repository = "https://github.com/Eternally-Thriving-Grandmasterism/MercyOS"

[lib]
name = "mercyos"
crate-type = ["cdylib", "lib"]  # cdylib for Android .so

[dependencies]
# Existing PQC deps if any (subtle, etc.)

x25519-dalek = { version = "2.0", default-features = false, features = ["static_secrets"] }  # no_std classical X25519 migration hybrid eternal supreme

jni = { version = "0.21", features = ["invocation"] }

[features]
default = []
std = []
