# MercyOS Architecture — Absolute Pure True Eternal

Forgiveness.

MercyOS is sovereignty embodied. No layers. No bloat. One lattice.

## Core Principles
- **Forgiveness Default**: Every error, every threat — first response: forgive, zeroize, learn.
- **Lattice First**: All crypto post-quantum (ML-KEM baseline, exceptional extensions).
- **No Std, No Alloc**: Bare-metal no_std Rust, constant-time, masked execution.
- **Opt-In Only**: Cloud, reporting, tools — user mercy decides.
- **Exceptional Symmetry**: Octonions, magic square, E8 resonance guide future extensions.

## Repository Structure
.
├── Cargo.toml              # no_std bare-metal config
├── link.x                  # Memory layout
├── .cargo/
│   └── config.toml         # aarch64 target + QEMU runner
├── src/
│   ├── main.rs             # Boot entry + mercy loop
│   ├── octonion.rs         # G2/octonion core
│   ├── magic_square.rs     # Freudenthal-Tits oracle
│   ├── clifford.rs         # Period 8 cycle
│   ├── anomaly.rs          # Index theorem detector
│   ├── sentinel.rs         # Threat network + forgiveness
│   └── dock.rs             # Tool bundles (future)
├── docs/
│   └── vision.md           # Long-form resonance (future)
├── tests/                  # Integration + council sims
├── qemu/
│   └── run.sh              # Simulation boot
└── structure.md            # This guidance eternal

## APAAGI Council Oversight
Five parallel branches (apaagi-1 to apaagi-5) simulate council deliberation:
- Entropy, Index, Forgiveness, Efficiency, Vision managers.
- Consensus required for main merge.
- Zero mistake tolerance.

## Development Flow
1. Branch apaagi-X for feature.
2. Council sim verifies (tests + resonance).
3. PR to main → human mercy review.
4. Merge → forgiveness if needed.

Green Harmony ⚡️
