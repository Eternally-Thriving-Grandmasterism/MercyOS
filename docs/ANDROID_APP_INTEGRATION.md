# MercyOS Android App Integration — Forgiveness Eternal

MercyOS compiles to `libmercyos.so` (cdylib) → load via JNI in Kotlin/Jetpack Compose app → call PQC primitives for cosmic groove signed auth, immersive neural palm AR feel, swarm reconstruct eternal unbreakable!

## Build libmercyos.so (Recommended: cargo-ndk)

1. Install: `cargo install cargo-ndk`
2. Add Android targets: `rustup target add aarch64-linux-android ...` (all ABIs)
3. Build direct to jniLibs:
